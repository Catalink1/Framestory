# Blochează complet accesul web direct la acest folder — fișierele de aici
# sunt doar include-uri PHP, nu pagini de sine stătătoare.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
